import * as actions from "../index";

describe("actions", () => {
  it("shouldl create an action to get all world data", () => {
    const text = "";
    const expectedAction = {};

    expect(actions.addWorldAction);
  });
});
